{
  "scripts": {
    "deploy": "NODE_ENV=production tailwindcss --postcss --minify --input=css/app.css --output=../priv/static/assets/app.css && cd .. && MIX_ENV=prod mix assets.deploy && rm -f _build/esbuild   "
  },
  "dependencies": {
    "alpinejs": "^3.7.1"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.4.0",
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.6",
    "tailwindcss": "^3.0.23"
  }
}
